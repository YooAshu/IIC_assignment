<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="BottomNavActiveIndicatorStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <!-- Customize the active indicator color and size here -->
        <item name="android:background">@drawable/activenav</item>
        <item name="android:layout_width">40dp</item> <!-- Adjust the width of the indicator -->
        <item name="android:layout_height">40dp</item> <!-- Adjust the height of the indicator -->
        <item name="color">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>